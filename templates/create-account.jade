extends layout

block content

  div.container

    h1 Create New User Account

    div.row
      div.col-md-7

        p Create a new account below. Use this account to add and manage properties on this site. All fields are required.

        form

          label
            | Your name:
            input.form-control(type="text" name="contactName") 

          label
            | Your phone number:
            input.form-control(type="text" name="contactPhone") 

          label
            | Your email address:
            input.form-control(type="text" name="email") 

          label
            | Password:
            input.form-control(type="password" name="password")

          label
            | Confirm password:
            input.form-control(type="password" name="password")

          input.btn.btn-primary.drop(type="submit" name="submit" value="Sign In") 


block scripts
